import {getAdminSupabase} from '@/lib/supabase';
export const dynamic='force-dynamic';
export default async function Dashboard(){let rows:any[]=[];let error='';try{const {data,error:e}=await getAdminSupabase().from('support_requests').select('*').order('created_at',{ascending:false});rows=data||[];if(e)error=e.message}catch(e){error='Set SUPABASE_SERVICE_ROLE_KEY on the server before using the dashboard.'}return <main className="container"><h1>Support dashboard</h1><p className="muted">Protect this route with Supabase Auth before production deployment.</p>{error?<div className="success">{error}</div>:<div className="tableWrap"><table className="table"><thead><tr><th>Platform</th><th>Contact</th><th>Issue</th><th>Status</th><th>Created</th></tr></thead><tbody>{rows.map(r=><tr key={r.id}><td><span className="pill">{r.platform}</span></td><td>{r.email}</td><td>{r.issue}</td><td>{r.status}</td><td>{new Date(r.created_at).toLocaleString()}</td></tr>)}</tbody></table></div>}</main>}
